[[supported-features-ubuntu]]
= 지원되는 {ubuntu} 기능
:revdate: 2026-05-21
:page-revdate: {revdate}


이 표는 {ubuntu} 클라이언트에서 다양한 기능의 사용 가능 여부를 정리한 것입니다.

[IMPORTANT]
====
클라이언트에서 실행하는 운영 체제는 운영 체제를 제공하는 조직이 지원합니다. {ubuntu}은 Canonical에서 지원됩니다.
====

이 표의 아이콘에는 다음과 같은 의미가 있습니다.

* {check} 이 기능은 사용 가능합니다.
* {cross} 이 기능은 사용할 수 없습니다.
* {question} 이 기능은 현재 고려 중이며, 이후 사용 가능 여부가 결정될 수 있습니다.

ifeval::[{mlm-content} == true]


[cols="1,1,1", options="header"]
.{ubuntu} 운영 체제에서 지원되는 기능
|===
| 기능
| {ubuntu}{nbsp}22.04
| {ubuntu}{nbsp}24.04

| 클라이언트
| {check}
| {check}

| 시스템 패키지
| {ubuntu} 커뮤니티
| {ubuntu} 커뮤니티

| 등록
| {check}
| {check}

| 패키지 설치
| {check}
| {check}

| 패치 적용
| {check}
| {check}

| 원격 명령
| {check}
| {check}

| 시스템 패키지 상태
| {check}
| {check}

| 시스템 사용자 정의 상태
| {check}
| {check}

| 그룹 사용자 정의 상태
| {check}
| {check}

| 조직 사용자 정의 상태
| {check}
| {check}

| 시스템 설정 관리자(SSM)
| {check}
| {check}

| 제품 마이그레이션
| N/A
| N/A

| 시스템 배포(PXE/Kickstart)
| {cross}
| {cross}

| 시스템 재배포 (Kickstart)
| {cross}
| {cross}

| 연락 방법
| {check} ZeroMQ, Salt-SSH
| {check} ZeroMQ, Salt-SSH

| {productname}프록시와 함께 작동
| {check}
| {check}

| 작업 체인
| {check}
| {check}

| 스테이징 (패키지 사전 다운로드)
| {check}
| {check}

| 중복 패키지 보고
| {check}
| {check}

| CVE 감사
| {check}
| {check}

| SCAP 감사
| {question}
| {question}

| 패키지 검증
| {cross}
| {cross}

| 패키지 잠금
| {check}
| {check}

| 유지보수 기간
| {check}
| {check}

| 시스템 잠금
| {cross}
| {cross}

| 시스템 스냅샷
| {cross}
| {cross}

| 구성 파일 관리
| {check}
| {check}

| 패키지 프로파일
| {check} 프로파일 지원, 동기화 미지원
| {check} 지원되는 프로파일, 동기화 미지원

| 전원 관리
| {check}
| {check}

| 모니터링 서버
| {cross}
| {cross}

| 모니터링된 클라이언트
| {check}
| {check}

| Docker 빌드호스트
| {question}
| {question}

| OS로 Docker 이미지 빌드
| {check}
| {check}

| Kiwi 빌드호스트
| {cross}
| {cross}

| OS로 Kiwi 이미지 빌드
| {cross}
| {cross}

| 반복 작업
| {check}
| {check}

| AppStreams
| N/A
| N/A

| 요미
| N/A
| N/A
|===

endif::[]


ifeval::[{uyuni-content} == true]


[cols="1,1,1", options="header"]
.{ubuntu} 운영 체제에서 지원되는 기능
|===
| 기능
| {ubuntu}{nbsp}22.04
| {ubuntu}{nbsp}24.04

| 클라이언트
| {check}
| {check}

| 시스템 패키지
| Canonical
| Canonical

| 등록
| {check}
| {check}

| 패키지 설치
| {check}
| {check}

| 패치 적용
| {check}
| {check}

| 원격 명령
| {check}
| {check}

| 시스템 패키지 상태
| {check}
| {check}

| 시스템 사용자 정의 상태
| {check}
| {check}

| 그룹 사용자 정의 상태
| {check}
| {check}

| 조직 사용자 정의 상태
| {check}
| {check}

| 시스템 설정 관리자(SSM)
| {check}
| {check}

| 제품 마이그레이션
| N/A
| N/A

| 시스템 배포(PXE/Kickstart)
| {cross}
| {cross}

| 시스템 재배포 (Kickstart)
| {cross}
| {cross}

| 연락 방법
| {check} ZeroMQ, Salt-SSH
| {check} ZeroMQ, Salt-SSH

| {productname}프록시와 함께 작동
| {check}
| {check}

| 작업 체인
| {check}
| {check}

| 스테이징 (패키지 사전 다운로드)
| {check}
| {check}

| 중복 패키지 보고
| {check}
| {check}

| CVE 감사
| {check}
| {check}

| SCAP 감사
| {question}
| {question}

| 패키지 검증
| {cross}
| {cross}

| 패키지 잠금
| {check}
| {check}

| 유지보수 기간
| {check}
| {check}

| 시스템 잠금
| {cross}
| {cross}

| 시스템 스냅샷
| {cross}
| {cross}

| 구성 파일 관리
| {check}
| {check}

| 패키지 프로파일
| {check} 프로파일 지원, 동기화 미지원
| {check} 지원되는 프로파일, 동기화 미지원

| 전원 관리
| {check}
| {check}

| 모니터링
| {check}
| {check}

| Docker 빌드호스트
| {question}
| {question}

| OS로 Docker 이미지 빌드
| {check}
| {check}

| Kiwi 빌드호스트
| {cross}
| {cross}

| OS로 Kiwi 이미지 빌드
| {cross}
| {cross}

| 반복 작업
| {check}
| {check}

| AppStreams
| N/A
| N/A

| 요미
| N/A
| N/A
|===

endif::[]
