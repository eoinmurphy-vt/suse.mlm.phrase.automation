= {productname} 서버 업그레이드
:description: 호스트 운영 체제를 업데이트하고 zypper를 사용하여 소프트웨어 리포지토리를 새로 고침하여 서버를 원활하게 업그레이드하도록 구성하십시오.
:revdate: 2026-02-16
:page-revdate: {revdate}
:page-author: SUSE 제품 및 솔루션 문서 팀
:page-image: https://www.suse.com/assets/img/suse-white-logo-green.svg
:page-product-name: SUSE Multi-Linux Manager
:page-product-version: 5.1

업그레이드 명령을 실행하기 전에 호스트 운영 체제를 업데이트해야 합니다.
호스트 운영 체제를 업데이트하면 {productname} 도구와 같은 [literal]``+mgradm+`` 도구도 업데이트됩니다.

.절차 서버 업그레이드
[role=procedure]
____

. [command]``+zypper+``로 소프트웨어 리포지토리 새로 고침:

+

[source,shell]
----
zypper ref
----

ifeval::[{mlm-content} == true]

. 호스트 운영 체제에 따라 다음 단계를 진행합니다.

+

{sl-micro}와 같은 트랜잭션 시스템의 경우: ::

+

--

. [command]``+transactional-update+``로 사용 가능한 업데이트 적용:

+

[source,shell]
----
transactional-update
----

. 업데이트가 적용된 경우 [literal]``+reboot+``.

--

+

endif::[]

ifeval::[{uyuni-content} == true]

. [command]``+transactional-update+``로 사용 가능한 업데이트 적용:

+

[source,shell]
----
transactional-update
----

. 업데이트가 적용된 경우 [literal]``+reboot+``.

endif::[]

ifeval::[{mlm-content} == true]

{sles}의 경우: ::

+

--

[command]``+zypper+``로 설치된 소프트웨어 업데이트:

[source,shell]
----
zypper up
----

--

endif::[]

+

. 다음 명령을 사용하여 {productname} 서버 컨테이너를 업데이트할 수 있습니다:

+

ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-ptf-loss-during-upgrade.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../partials/snippet-ptf-loss-during-upgrade.adoc[]
endif::[]

+

[source,shell]
----
mgradm upgrade podman
----

+

이 명령은 컨테이너의 상태를 최신 상태로 가져오고 서버를 다시 시작합니다.

+
. 사용하지 않는 컨테이너 이미지를 정리하여 디스크 공간을 확보합니다.

+
[source,shell]
----
podman image prune -a
----

+

ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-check-rpmnew-rpmsave.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../partials/snippet-check-rpmnew-rpmsave.adoc[]
endif::[]

____


.가져온 인증서 제거
[NOTE]
====
이전 버전에서는 데이터베이스 컨테이너에 다음 주체 대체 이름(SAN)이 포함된 SSL 인증서가 필요했습니다:

* [literal]``+db+``
* [literal]``+reportdb+``

더 이상 필요하지 않습니다.
데이터베이스 컨테이너는 외부에 노출된 완전한 도메인 이름만 필요합니다.
[literal]``+db+``+ 및 [literal]+``+reportdb+`` SAN이 포함된 이전 인증서는 여전히 사용할 수 있습니다.

같은 인증서를 메인 컨테이너와 데이터베이스 컨테이너 모두에 사용할 수 있습니다.

업그레이드 명령에 새 인증서를 전달하려면 [command]``+--ssl-db-ca-root+``+, [command]+``+--ssl-db-cert+``+ 및 [command]+``+--ssl-db-key+`` 매개변수를 사용합니다.
====

.특정 버전으로 업그레이드
[NOTE]
====
태그 파라미터를 지정하지 않으면 가장 최근 버전으로 업그레이드하는 것으로 기본 설정됩니다.
특정 버전으로 업그레이드하려면 원하는 이미지 태그와 함께 태그 파라미터를 제공하십시오.
====

업그레이드 명령과 해당 파라미터에 대한 자세한 내용은 다음 명령을 참조하십시오.

ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-ptf-loss-during-upgrade.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../partials/snippet-ptf-loss-during-upgrade.adoc[]
endif::[]

[source,shell]
----
mgradm upgrade podman -h
----


air-gapped 설치의 경우, 먼저 컨테이너 RPM 패키지를 업그레이드한 후 [literal]``+mgradm+`` 명령을 실행합니다.



ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-database-backup-volume.adoc[leveloffset=+1]
endif::[]

ifdef::backend-pdf[]
include::../../partials/snippet-database-backup-volume.adoc[leveloffset=+1]
endif::[]
