[[supported-features-opensuse-tumbleweed]]
= {opensuse} {tumbleweed} 客户端功能
:revdate: 2025-10-08
:page-revdate: {revdate}


[IMPORTANT]
====
{opensuse} {tumbleweed} 由 {suse} 社区支持。
====


此表中图标的含意如下：

* {check} 该功能可用
* {cross} 该功能不可用
* {question} 该功能正在考虑之中，日后可能提供，也可能不提供


[cols="1,1", options="header"]
.{opensuse} {tumbleweed} 操作系统上支持的功能
|===

| 功能
| {opensuse} {tumbleweed}

| 客户端
| {check}

| 操作系统包
| {check}

| 注册
| {check}

| 安装软件包
| {check}

| 应用补丁（需要 CVE ID）
| {check}

| 远程命令
| {check}

| 系统软件包状态
| {check}

| 系统自定义状态
| {check}

| 组自定义状态
| {check}

| 组织自定义状态
| {check}

| 系统集管理器（SSM）
| {check}

| 产品迁移
| {check}

| 基本虚拟 Guest 管理 {star}
| {check}

| 高级虚拟 Guest 管理 {star}
| {check}

| 虚拟 Guest 安装（Kickstart），作为主机操作系统
| {check}

| 虚拟 Guest 安装（镜像模板），作为主机操作系统
| {check}

| 系统部署（PXE 引导/Kickstart）
| {check}

| 系统重新部署（Kickstart）
| {check}

| 联系方式
| {check} ZeroMQ

| 与 {productname} 代理兼容
| {check}

| 操作链
| {check}

| 暂存 (软件包预下载)
| {question}

| 重复软件包报告
| {check}

| CVE 审计（需要 CVE ID）
| {check}

| SCAP 审计
| {question}

| 软件包校验
| {question}

| 软件包锁定
| {check}

| 系统锁定
| {question}

| 维护时段
| {question}

| 系统快照
| {cross}

| 配置文件管理
| {check}

| 快照和控制文件
| {check}支持的控制文件，不支持同步

| 电源管理
| {check}

| 监视服务器
| {cross}

| 监控的客户端
| {check}

| Docker 构建主机
| {cross}

| 使用操作系统构建 Docker 镜像
| {cross}

| Kiwi 构建主机
| {cross}

| 使用操作系统构建 Kiwi 镜像
| {check}

| 重复性操作
| {check}

| AppStream
| 不适用

| Yomi
| {question}
|===

{star} 虚拟 Guest 管理：

在此表格中，虚拟 Guest 管理分为基本管理和高级管理。

基本虚拟 Guest 管理包括列出 VM、慢速刷新、VM 生命周期操作（开始、停止、继续、暂停）以及修改 VM vCPU 和内存。

高级虚拟 Guest 管理包括基本虚拟 Guest 管理的所有功能以及快速刷新、VM 生命周期操作（删除、重置、关机）、修改 VM 磁盘、网络、图形显示和图形显示配置。
