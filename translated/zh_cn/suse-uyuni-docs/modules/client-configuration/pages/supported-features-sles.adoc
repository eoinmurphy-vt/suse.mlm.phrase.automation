[[supported-features-sles]]
= 支持的 {slsa} 和 {opensuse} 功能
:description: 比较不同操作系统变体中 SUSE 和 openSUSE 客户端的功能可用性
:revdate: 2024-05-10
:page-revdate: {revdate}
:page-author: SUSE 产品与解决方案文档团队
:page-image: https://www.suse.com/assets/img/suse-white-logo-green.svg
:page-product-name: SUSE Multi-Linux Manager
:page-product-version: 5.1


下表列出了 {suse} 和 {opensuse} 客户端上各种功能的可用性。
该表涵盖了 {sle} 操作系统的所有变体，包括 {slsa}、{sleda}、{sles} 用于 SAP，以及 {sles} 用于 HPC。

[IMPORTANT]
====
客户端上运行的操作系统由提供操作系统的组织支持。

* {sle} 由 {suse} 支持。
** {suse} 仅支持处于一般支持或长期支持 (LTS) 的产品。
* {opensuse} 由 {suse} 社区支持。

====

此表中图标的含意如下：

* {check} 该功能可用
* {cross} 该功能不可用
* {question} 该功能正在考虑之中，日后可能提供，也可能不提供


[cols="1,1,1,1,1,1", options="header"]
.{suse} 和 {opensuse} 操作系统上支持的功能
|===

| 功能
| {sle}{nbsp}12
| {sle}{nbsp}15
| {sle}{nbsp}16
| {leap} 15
| {leap} 16

| 客户端
| {check}
| {check}
| {check}
| {check}
| {check}

| 系统软件包
| {suse}
| {suse}
| {suse}
| {opensuse} 社区
| {opensuse} 社区

| 注册
| {check}
| {check}
| {check}
| {check}
| {check}

| 安装软件包
| {check}
| {check}
| {check}
| {check}
| {check}

| 应用补丁
| {check}
| {check}
| {check}
| {check}
| {check}

| 远程命令
| {check}
| {check}
| {check}
| {check}
| {check}

| 系统软件包状态
| {check}
| {check}
| {check}
| {check}
| {check}

| 系统自定义状态
| {check}
| {check}
| {check}
| {check}
| {check}

| 组自定义状态
| {check}
| {check}
| {check}
| {check}
| {check}

| 组织自定义状态
| {check}
| {check}
| {check}
| {check}
| {check}

| 系统集管理器（SSM）
| {check}
| {check}
| {check}
| {check}
| {check}

| 产品迁移
| {check}
| {check}
| {question}
| {check}
| {question}

| 系统部署（PXE 引导/{ay}）
| {check}
| {check}
| {question}
| {check}
| {question}

| 系统重新部署（{ay}）
| {check}
| {check}
| {question}
| {cross}
| {question}

| 联系方法
| {check} ZeroMQ, Salt-SSH
| {check} ZeroMQ, Salt-SSH
| {check} ZeroMQ, Salt-SSH
| {check} ZeroMQ, Salt-SSH
| {check} ZeroMQ, Salt-SSH

| 操作链
| {check}
| {check}
| {check}
| {check}
| {check}

| 暂存（预下载软件包）
| {check}
| {check}
| {check}
| {check}
| {check}

| 重复软件包报告
| {check}
| {check}
| {check}
| {check}
| {check}

| CVE 审计
| {check}
| {check}
| {check}
| {check}
| {check}

| SCAP 审计
| {check}
| {check}
| {question}
| {check}
| {question}

| 软件包锁定
| {check}
| {check}
| {check}
| {check}
| {check}

| 系统锁定
| {cross}
| {cross}
| {cross}
| {cross}
| {cross}

| 维护时段
| {check}
| {check}
| {check}
| {check}
| {check}

| 系统快照
| {cross}
| {cross}
| {cross}
| {cross}
| {cross}

| 配置文件管理
| {check}
| {check}
| {check}
| {check}
| {check}

| 软件包配置文件
| {check} 支持的配置文件，不支持同步
| {check} 支持的配置文件，不支持同步
| {check} 支持的配置文件，不支持同步
| {check} 支持的配置文件，不支持同步
| {check} 支持的配置文件，不支持同步

| 电源管理
| {check}
| {check}
| {check}
| {check}
| {check}

| 监视服务器
| {cross}
| {check}
| {question}
| {check}
| {question}

| 监控的客户端
| {check}
| {check}
| {question}
| {check}
| {question}

| Docker 构建主机
| {check}
| {check}
| {question}
| {question}
| {question}

| 使用操作系统构建 Docker 镜像
| {check}
| {check}
| {check}
| {check}
| {check}

| Kiwi 构建主机
| {check}
| {question}
| {question}
| {question}
| {question}

| 使用操作系统构建 Kiwi 镜像
| {check}
| {question}
| {question}
| {cross}
| {cross}

| 重复性操作
| {check}
| {check}
| {check}
| {check}
| {check}

| AppStream
| 不适用
| 不适用
| 不适用
| 不适用
| 不适用

| Yomi
| {cross}
| {cross}
| {cross}
| {cross}
| {cross}

|===

