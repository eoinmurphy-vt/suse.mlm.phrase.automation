[[supported-features-centos]]
= サポートされているCentOSの機能
:revdate: 2024-09-30
:page-revdate: {revdate}


この表には、{CentOS}クライアントのさまざまな機能の使用可否がリストされています。


[IMPORTANT]
====
クライアントで実行しているオペレーティングシステムは、オペレーティングシステムを提供している組織によってサポートされています。{centos}は{centos}コミュニティでサポートされています。
====

[NOTE]
====
{centos} 7 のサポートは、{sll} 7 を対象とした移行シナリオに限定されています。スタンドアロンでの使用はサポートされなくなりました。
====

この表のアイコンの意味は次のとおりです。

* {check}: 機能は使用可能です
* {cross}: 機能は使用できません
* {question}: 機能は検討中であり、後日利用可能になるかどうかは未定です


[cols="1,1", options="header"]
.{centos}オペレーティングシステムでサポートされている機能
|===

| 機能
| {centos}{nbsp}7

| [クライアント]
| {check}（プレーンCentOS）

| システムパッケージ
| {centos}コミュニティ

| 登録
| {check}

| パッケージのインストール
| {check}

| パッチを適用します（CVE IDが必要です）
| {check}（エラッタにはサードパーティサービスが必要です）

| リモートコマンド
| {check}

| システムパッケージの状態
| {check}

| システムカスタム状態
| {check}

| グループカスタム状態
| {check}

| 組織カスタム状態
| {check}

| システムセットマネージャ（SSM）
| {check}

| 製品の移行 {star}
| {check}

| システムデプロイメント（PXE/Kickstart）
| {check}

| システム再デプロイメント（Kickstart）
| {check}

| 連絡方法
| {check} ZeroMQ、Salt-SSH

| {productname} プロキシと連携
| {check}

| アクションチェーン
| {check}

| ステージング（パッケージの事前ダウンロード）
| {check}

| 重複パッケージの報告
| {check}

| CVE監査（CVE IDが必要）
| {check}

| SCAP監査
| {check}

| パッケージ検証
| {cross}

| パッケージのロック
| {check}

| システムのロック
| {cross}

| メンテナンスウィンドウ
| {check}

| システムスナップショット
| {cross}

| 設定ファイル管理
| {check}

| スナップショットとプロファイル
| {check} プロファイルはサポートされていますが、syncはサポートされていません。

| 電源管理
| {check}

| モニタリングサーバ
| {cross}

| 監視対象のクライアント
| {check}

| Dockerビルドホスト
| {cross}

| OSを使用してDockerイメージをビルド
| {cross}

| Kiwiビルドホスト
| {cross}

| OSを使用してKiwiイメージをビルド
| {cross}

| 定期的なアクション
| {check}

| AppStream
| N/A

| 読み
| N/A

|===

{star}の製品移行は、対応する{sll}バージョンへの移行となります。
