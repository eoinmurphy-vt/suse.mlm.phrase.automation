[[supported-features-rh]]
= サポートされている{rhel}機能
:revdate: 2026-05-21
:page-revdate: {revdate}

ifeval::[{mlm-content} == true]

// SUSE Liberty Linux not available at Uyuni for now
この表には、ネイティブ{rhel}クライアントのさまざまな機能の使用可否がリストされています（拡張サポートなし）。
endif::[]

ifeval::[{uyuni-content} == true]

// SUSE Liberty Linux not available at Uyuni for now
この表には、ネイティブ{rhel}クライアントのさまざまな機能の使用可否がリストされています。
endif::[]


[IMPORTANT]
====
クライアントで実行しているオペレーティングシステムは、オペレーティングシステムを提供している組織によってサポートされています。{rhel}は{redhat}でサポートされています。
====

この表のアイコンの意味は次のとおりです。

* {check}: 機能は使用可能です
* {cross}: 機能は使用できません
* {question}: 機能は検討中であり、後日利用可能になるかどうかは未定です


[cols="1,1,1,1,1", options="header"]
.{rhel}オペレーティングシステムでサポートされている機能
|===
| 機能
| {rhela}{nbsp}7
| {rhela}{nbsp}8
| {rhela}{nbsp}9
| {rhela}{nbsp}10

| [クライアント]
| {check}
| {check}
| {check}
| {check}

| システムパッケージ
| {redhat}
| {redhat}
| {redhat}
| {redhat}

| 登録
| {check}
| {check}
| {check}
| {check}

| パッケージのインストール
| {check}
| {check}
| {check}
| {check}

| パッチの適用
| {check}
| {check}
| {check}
| {check}

| リモートコマンド
| {check}
| {check}
| {check}
| {check}

| システムパッケージの状態
| {check}
| {check}
| {check}
| {check}

| システムカスタム状態
| {check}
| {check}
| {check}
| {check}

| グループカスタム状態
| {check}
| {check}
| {check}
| {check}

| 組織カスタム状態
| {check}
| {check}
| {check}
| {check}

| システムセットマネージャ（SSM）
| {check}
| {check}
| {check}
| {check}

| 製品移行{star}
| N/A
| N/A
| N/A
| N/A

| システムデプロイメント（PXE/Kickstart）
| {check}
| {check}
| {check}
| {check}

| システム再デプロイメント（Kickstart）
| {check}
| {check}
| {check}
| {check}

| 連絡方法
| {check} ZeroMQ、Salt-SSH
| {check} ZeroMQ、Salt-SSH
| {check} ZeroMQ、Salt-SSH
| {check} ZeroMQ、Salt-SSH

| {productname} プロキシと連携
| {check}
| {check}
| {check}
| {check}

| アクションチェーン
| {check}
| {check}
| {check}
| {check}

| ステージング（パッケージの事前ダウンロード）
| {check}
| {check}
| {check}
| {check}

| 重複パッケージの報告
| {check}
| {check}
| {check}
| {check}

| CVE監査
| {check}
| {check}
| {check}
| {check}

| SCAP監査
| {check}
| {check}
| {check}
| {check}

| パッケージロック
| {check}
| {check}
| {check}
| {check}

| システムのロック
| {cross}
| {cross}
| {cross}
| {cross}

| メンテナンスウィンドウ
| {check}
| {check}
| {check}
| {check}

| システムスナップショット
| {cross}
| {cross}
| {cross}
| {cross}

| 設定ファイル管理
| {check}
| {check}
| {check}
| {check}

| スナップショットとプロファイル
| {check} プロファイルはサポートされていますが、syncはサポートされていません。
| {check} プロファイルはサポートされていますが、syncはサポートされていません。
| {check} プロファイルはサポートされていますが、syncはサポートされていません。
| {check} プロファイルはサポートされていますが、syncはサポートされていません。

| 電源管理
| {check}
| {check}
| {check}
| {check}

| モニタリングサーバ
| {cross}
| {cross}
| {cross}
| {cross}

| 監視対象のクライアント
| {check}
| {check}
| {check}
| {check}

| Dockerビルドホスト
| {check}
| {check}
| {check}
| {check}

| OSを使用してDockerイメージをビルド
| {question}
| {question}
| {question}
| {question}

| Kiwiビルドホスト
| {cross}
| {cross}
| {cross}
| {cross}

| OSを使用してKiwiイメージをビルド
| {cross}
| {cross}
| {cross}
| {cross}

| 定期的なアクション
| {check}
| {check}
| {check}
| {check}

| AppStream
| N/A
| {check}
| {check}
| {check}

| 読み
| N/A
| N/A
| N/A
| N/A
|===

{star}の製品移行は、対応する{sll}バージョンへの移行となります。
