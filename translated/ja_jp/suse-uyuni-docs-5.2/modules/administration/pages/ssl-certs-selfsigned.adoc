[[ssl-certs-selfsigned]]
= 自己署名SSL証明書
:revdate: 2026-06-12
:page-revdate: {revdate}


デフォルトでは、{productname}は自己署名証明書を使用します。
この場合、証明書は{productname}によって作成され、署名されます。
この方法では、証明書の詳細が正しいことを保証するために独立したCAを使用しません。
第三者のCAは、証明書に含まれる情報が正しいことを確認するためのチェックを行います。

* 第三者のCAに関する詳細は、xref:administration:ssl-certs-imported.adoc[]を参照してください。
* 証明書の置き換えに関する詳細は、xref:administration:ssl-certs-imported.adoc#ssl-certs-import-replace[]を参照してください。

このセクションでは、新規または既存のインストールで自己署名証明書を作成または再作成する方法について説明します。

SSLキーおよび証明書のホスト名はそれらを配備するマシンの完全修飾ホスト名に一致する必要があります。

[IMPORTANT]
====
古い{productname}バージョンで生成されたCA証明書には、`+X509v3 Basic Constraints+`拡張機能の`+critical+`フラグが欠落している場合があります。
Python 3.13以降を含む厳格な証明書バリデーターは、そのようなCA証明書を拒否し、`+Basic Constraints of CA cert not marked critical+`を報告する可能性があります。
自己署名CAが影響を受ける場合は、新しいCAと新しいサーバー証明書を作成し、新しいルートCAをすべてのクライアントにデプロイしてください。
CA証明書の確認について詳しくは、xref:administration:ssl-certs-imported.adoc#ssl-certs-verify-ca-basic-constraints[]を参照してください。
証明書の置き換えおよびクライアントへのルートCAのデプロイについて詳しくは、xref:administration:ssl-certs-imported.adoc#ssl-certs-import-deploy-root-ca[]を参照してください。
====


== 既存のサーバ証明書の再作成


既存の証明書の有効期限が切れているか、何らかの理由で動作を停止している場合は、既存のCAから新しいサーバ証明書を生成できます。

.プロシージャ:既存のサーバ証明書の再作成

. {productname}コンテナホストのコマンドプロンプトで、サーバ証明書を再生成します:

+

[source,shell]
----
mgrctl exec -ti -- 'rhn-ssl-tool --gen-server --dir="/root/ssl-build" --set-country="COUNTRY" \
--set-state="STATE" --set-city="CITY" --set-org="ORGANIZATION" \
--set-org-unit="ORGANIZATION UNIT" --set-email="name@example.com" \
--set-hostname="susemanager.example.com" --set-cname="example.com"'
----

+

`+set-cname+`パラメータが、お使いの{productname}サーバーの完全修飾ドメイン名であることを確認してください。
複数のエイリアスが必要な場合は、`+set-cname+`パラメータを複数回使用できます。

秘密鍵とサーバ証明書は、サーバコンテナ内の`+/root/ssl-build/susemanager/+`ディレクトリに、それぞれ`+server.key+`および`+server.crt+`として格納されています。
最後のディレクトリの名前は、`+--set-hostname+`オプションで使用されるホスト名によって異なります。

コンテナのホストpodmanシークレットを更新して、新しい証明書とキーをデプロイまたはインポートします。
生成したばかりの証明書のインポートに関する詳細は、xref:administration:ssl-certs-imported.adoc#ssl-certs-import-replace[]を参照してください。



[[ssl-certs-selfsigned-create-replace]]
== 新しいCAおよびサーバ証明書の作成

[IMPORTANT]
====
ルートCAを置き換える必要がある場合は注意してください。
サーバとクライアント間の信頼チェーンが破損する可能性があります。
その場合、すべてのクライアントにログインし、CAを直接デプロイするために管理者ユーザーが必要です。
====


.プロシージャ:新しい証明書の作成

. {productname}コンテナホストのコマンドプロンプトで、古い証明書ディレクトリを新しい場所に移動します。

+

[source,shell]
----
mgrctl exec -- mv /root/ssl-build /root/old-ssl-build
----

. 新しいCA証明書を生成します。

+

[source,shell]
----
mgrctl exec -ti -- 'rhn-ssl-tool --gen-ca --dir="/root/ssl-build" --set-country="COUNTRY" \
--set-state="STATE" --set-city="CITY" --set-org="ORGANIZATION" \
--set-org-unit="ORGANIZATION UNIT" --set-common-name="SUSE Manager CA Certificate" \
--set-email="name@example.com"'
----

+

. 新しいサーバ証明書を生成します。

+

[source,shell]
----
mgrctl exec -ti -- 'rhn-ssl-tool --gen-server --dir="/root/ssl-build" --set-country="COUNTRY" \
--set-state="STATE" --set-city="CITY" --set-org="ORGANIZATION" \
--set-org-unit="ORGANIZATION UNIT" --set-email="name@example.com" \
--set-hostname="susemanager.example.top" --set-cname="example.com"'
----

+

`+set-cname+`パラメータが、お使いの{productname}サーバーの完全修飾ドメイン名であることを確認してください。
複数のエイリアスが必要な場合は、`+set-cname+`パラメータを複数回使用できます。
+
ホスト名とcnameを使用して、各プロキシのサーバ証明書も生成する必要があります。
