[[supported-features-opensuse-tumbleweed]]
= {opensuse} {tumbleweed} 클라이언트 기능
:revdate: 2025-10-08
:page-revdate: {revdate}


[IMPORTANT]
====
{opensuse} {tumbleweed}은(는) {suse} 커뮤니티에서 지원됩니다.
====


이 표의 아이콘에는 다음과 같은 의미가 있습니다.

* {check} 이 기능은 사용 가능합니다.
* {cross} 이 기능은 사용할 수 없습니다.
* {question} 이 기능은 현재 고려 중이며, 이후 사용 가능 여부가 결정될 수 있습니다.


[cols="1,1", options="header"]
.{opensuse} {tumbleweed} 운영 체제에서 지원되는 기능
|===

| 기능
| {opensuse} {tumbleweed}

| 클라이언트
| {check}

| 운영 체제 패키지
| {check}

| 등록
| {check}

| 패키지 설치
| {check}

| 패치 적용(필요한 CVE ID)
| {check}

| 원격 명령
| {check}

| 시스템 패키지 상태
| {check}

| 시스템 사용자 정의 상태
| {check}

| 그룹 사용자 정의 상태
| {check}

| 조직 사용자 정의 상태
| {check}

| 시스템 설정 관리자(SSM)
| {check}

| 제품 마이그레이션
| {check}

| 기본 가상 게스트 관리 {star}
| {check}

| 고급 가상 게스트 관리 {star}
| {check}

| 호스트 OS로서의 가상 게스트 설치 (Kickstart)
| {check}

| 호스트 OS로서의 가상 게스트 설치 (이미지 템플릿)
| {check}

| 시스템 배포 (PXE/Kickstart)
| {check}

| 시스템 재배포 (Kickstart)
| {check}

| 연락 방법
| {check} ZeroMQ

| {productname}프록시와 함께 작동
| {check}

| 작업 체인
| {check}

| 스테이징 (패키지 사전 다운로드)
| {question}

| 중복 패키지 보고
| {check}

| CVE 감사 (CVE ID 필요)
| {check}

| SCAP 감사
| {question}

| 패키지 검증
| {question}

| 패키지 잠금
| {check}

| 시스템 잠금
| {question}

| 유지보수 기간
| {question}

| 시스템 스냅샷
| {cross}

| 구성 파일 관리
| {check}

| 스냅샷 및 프로파일
| {check} 지원되는 프로파일, 동기화 미지원

| 전원 관리
| {check}

| 모니터링 서버
| {cross}

| 모니터링된 클라이언트
| {check}

| Docker 빌드호스트
| {cross}

| OS로 Docker 이미지 빌드
| {cross}

| Kiwi 빌드호스트
| {cross}

| OS로 Kiwi 이미지 빌드
| {check}

| 반복 작업
| {check}

| AppStreams
| N/A

| 요미
| {question}
|===

{star} 가상 게스트 관리:

이 표에서 가상 게스트 관리는 기본과 고급으로 구분되어 있습니다.

기본 가상 게스트 관리에는 VM, 느린 새로 고침, VM 라이프싸이클 작업(시작, 중지, 다시 시작, 일시 중지) 나열과 VM vCPU 및 메모리 수정이 포함됩니다.

고급 가상 게스트 관리에는 기본 가상 게스트 관리의 모든 기능과 함께 빠른 새로 고침, VM 라이프싸이클 작업(삭제, 재설정, 전원 끄기), VM 디스크, 네트워크, 그래픽 표시 및 그래픽 표시 구성 수정이 포함됩니다.
